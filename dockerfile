# Используем официальный образ Python 3.9 (slim - уменьшенная версия)
FROM python:3.9-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем наш HTML файл в рабочую директорию контейнера
COPY index.html .

# Сообщаем Docker, что контейнер будет слушать порт 8000
EXPOSE 8000

# Команда, которая будет выполняться при запуске контейнера:
# Запускаем встроенный HTTP сервер Python на порту 8000,
# доступный со всех сетевых интерфейсов внутри контейнера (0.0.0.0)
CMD ["python", "-m", "http.server", "8000", "--bind", "0.0.0.0"]